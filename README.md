## nvim-config
Config set-up for Neovim 0.12

### Personal nvim configuration repository.
 - trying to migrate configuration from branch 'java-config' to the native vim.pack package manager


### Terminal tools dependencies, make sure you install these before applying this config:
 - ripgrep
 - fd
 - fzf
 - lazygit
